# nazuraki.github.io — Purpose

Personal portfolio and project index, deployed as a GitHub Pages static site.

## What it does

Fetches all public, non-forked repositories from the GitHub API at build time and renders them as a curated project ledger. Repos with a matching screenshot in `public/screenshots/<repo>.png` get a featured card with preview image; the rest render in a compact secondary grid.
